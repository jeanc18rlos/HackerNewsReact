import React from 'react';
import loader from './animation.apng';
const Loader = props => {
    return <div style={{textAlign:"center"}}>
        <img src={loader} />
    </div>
}
export default Loader;